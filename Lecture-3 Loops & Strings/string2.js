let obj = {
    item: "pen",
    price: 10,
};

let output = `the cost of ${obj.item} is ${obj.price} ruupes`;
console.log(output);

console.log("This cost of",obj.item," is",obj.price,"rupees");

//Template literals
let specialString = `This is a template literal`;
console.log(typeof specialString);
